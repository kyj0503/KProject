<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>카카오 지도 및 사이드바</title>

    <!-- 통합된 CSS -->
    <link rel="stylesheet" href="/css/map.css">
    <link rel="stylesheet" href="/css/style.css">


    <!-- JavaScript 변수 설정 -->
    <script>
        const kakaoJsKey = "{{kakaoJsKey}}";  // 카카오 JavaScript API 키
        const kakaoRestKey = "{{kakaoRestKey}}";  // 카카오 REST API 키
        const isLoggedIn = {{isLoggedIn}};  // 로그인 여부
    </script>

    <!-- 카카오 지도 API -->
    <script src="//dapi.kakao.com/v2/maps/sdk.js?appkey={{kakaoJsKey}}&libraries=services,drawing"></script>
</head>
<body>
<div class="layout">
    <!-- 사이드바 -->
        <div class="sidebar">
            <button class="sidebar-btn">
                <img src="/img/home.png" alt="홈">
                <span>홈</span>
            </button>


            <button class="sidebar-btn">
                <img src="/img/way.png" alt="경로 탐색">
                <span>경로 탐색</span>
            </button>

            <button class="sidebar-btn">
                <img src="/img/like.png" alt="평점순">
                <span>평점순</span>
            </button>
{{#isLoggedIn}}
                <button class="sidebar-btn">
                    <img src="/img/mypage.png" alt="마이페이지">
                    <span>마이페이지</span>
                </button>
{{/isLoggedIn}}
{{^isLoggedIn}}

            <button class="sidebar-btn">
                <img src="/img/login.png" alt="로그인">
                <span>로그인</span>
            </button>
{{/isLoggedIn}}
    </div>

    <!-- 도어 -->
    <div class="door"></div>

    <!-- 지도 영역 -->
    <div class="map-container">
        <div id="map" class="map"></div>
    </div>
    <input type="hidden" id="keyword-data" value="도서관">
</div>





<!-- JavaScript -->

<script src="/js/map.js"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>


