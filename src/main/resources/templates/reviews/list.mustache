<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>평점순 장소 리스트</title>
    <link rel="stylesheet" href="/css/list.css">
</head>
<body>
{{>header}}
<div class="container my-5">
    <h1 class="mb-4">평점순 장소 리스트</h1>

    <!-- 테이블을 감싸는 div 추가 -->
    <div class="table-wrapper">
        <table class="table table-striped">
            <thead>
            <tr>
                <th>장소 ID</th>
                <th>장소 이름</th>
                <th>평균 평점</th>
                <th>리뷰 작성</th>
            </tr>
            </thead>
            <tbody>
            <!-- placesWithRatings 배열을 순회하여 각 장소와 관련된 정보를 표시 -->
            {{#placesWithRatings}}
                <tr>
                    <td>{{placeId}}</td>
                    <td>{{placeName}}</td>
                    <td>{{averageRating}}</td>
                    <td>
                        <a href="/reviews/create/{{placeId}}" class="btn btn-primary btn-sm">리뷰 남기기</a>
                    </td>
                </tr>
            {{/placesWithRatings}}
            </tbody>
        </table>
    </div>
</div>

{{>footer}}
</body>
</html>
